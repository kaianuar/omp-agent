# Fully-Automated Engineering Agent (omp-native) — MVP scaffold

This is the starting skeleton for an omp-orchestrated pipeline that takes a goal
and drives it to completion with two hard gates (tests + adversarial review) and a
human steer checkpoint.

Read `PIPELINE.md` first — it's the instruction file omp loads as its operating loop.
